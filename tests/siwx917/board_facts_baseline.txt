SIWX917_AI_DEV_KIT/manifest/psram  # danger: the IDE offers gpio [52, 53, 54, 55, 56, 57] as free expansion pins, but they are the PSRAM bus (mcu/boards/siwx917_ai_dev_kit/sl_si91x_psram_pin_config.h (PSRAM_GPIO_PIN_SET_52_TO_57)). Code runs from PSRAM, so muxing one of these away stops the CPU fetching the instructions doing it
SIWX917_AI_DEV_KIT/role/LEDG  # error: BOARD_LEDG_PIN = 51 is named for a led but is only used at siwx917_ai_board.c:143 in __board_register_display; the name and the wiring disagree
slcc/siwx917_ai_dev_kit.slcc/root_path  # error: slc/si91x_component/board/silabs/component/siwx917_ai_dev_kit.slcc declares root_path components/board/silabs/config/siwx917_ai_dev_kit, which does not exist, so nothing it lists can be found
slcc/siwx917_ai_dev_kit_config.slcc/config_file  # error: slc/si91x_component/board/silabs/component/siwx917_ai_dev_kit_config.slcc: 33 of its config_file paths do not exist, starting with common_config/sl_si91x_adc_init_channel_1_config.h. SLC resolves these only when the overridden component is in the project, so they fail whenever someone adds one
SIWX917_AI_DEV_KIT/manifest/conditional/29  # note: the IDE offers gpio 29 as a free expansion pin; the board claims it at BOARD_LCD_SPI_DC_PIN under "defined(DISPLAY_NAME) && ENABLE_TDD_DISP_ST7789", so the two agree only while that is off
SIWX917_AI_DEV_KIT/manifest/conditional/30  # note: the IDE offers gpio 30 as a free expansion pin; the board claims it at BOARD_LCD_BL_PIN under "defined(DISPLAY_NAME) && ENABLE_TDD_DISP_ST7789", so the two agree only while that is off
SIWX917_AI_DEV_KIT/manifest/conditional/51  # note: the IDE offers gpio 51 as a free expansion pin; the board claims it at BOARD_LEDG_PIN under "defined(DISPLAY_NAME) && ENABLE_TDD_DISP_ST7789", so the two agree only while that is off
BRD2605A/unused/LEDG  # warn: Kconfig declares BOARD_LEDG_PIN = 51 but no board source references it, so nothing is ever registered on that pin
BRD2605A/unused/SW3  # warn: Kconfig declares BOARD_SW3_PIN = 3 but no board source references it, so nothing is ever registered on that pin
SIWX917_AI_DEV_KIT/manifest/missing/LEDG  # warn: the board wires BOARD_LEDG_PIN = 51 but the IDE manifest has no entry for it, so the IDE does not show it
SIWX917_AI_DEV_KIT/unused/SW3  # warn: Kconfig declares BOARD_SW3_PIN = 3 but no board source references it, so nothing is ever registered on that pin
